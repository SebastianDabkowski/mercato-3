@page
@model MercatoApp.Pages.Admin.CommissionRules.IndexModel
@using MercatoApp.Models
@{
    ViewData["Title"] = "Commission Rules - Admin";
}

<div class="container-fluid my-4">
    <div class="row mb-4">
        <div class="col">
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item"><a href="/Admin/Dashboard">Admin</a></li>
                    <li class="breadcrumb-item active" aria-current="page">Commission Rules</li>
                </ol>
            </nav>
            <h1 class="mb-3">
                <i class="bi bi-percent"></i> Commission Rules Management
            </h1>
            <p class="text-muted">
                Configure commission rates and fees with effective dates and applicability rules.
            </p>
        </div>
    </div>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle"></i> @TempData["SuccessMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    @if (TempData["ErrorMessage"] != null)
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <i class="bi bi-exclamation-triangle"></i> @TempData["ErrorMessage"]
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <!-- Future Rules Alert -->
    @if (Model.FutureRules.Any())
    {
        <div class="alert alert-info" role="alert">
            <i class="bi bi-calendar-event"></i>
            <strong>Future-Dated Rules:</strong> 
            @Model.FutureRules.Count rule(s) scheduled to become effective in the future.
        </div>
    }

    <!-- Actions Bar -->
    <div class="row mb-3">
        <div class="col-md-6">
            <a href="/Admin/CommissionRules/Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> Create New Rule
            </a>
        </div>
        <div class="col-md-6 text-end">
            <div class="btn-group" role="group">
                <a href="?filter=all" class="btn btn-outline-secondary @(Model.Filter == "all" ? "active" : "")">
                    All Rules
                </a>
                <a href="?filter=active" class="btn btn-outline-secondary @(Model.Filter == "active" ? "active" : "")">
                    Active Only
                </a>
            </div>
        </div>
    </div>

    <!-- Commission Rules Table -->
    <div class="card">
        <div class="card-header bg-primary text-white">
            <h5 class="mb-0">
                <i class="bi bi-list-ul"></i> Commission Rules
            </h5>
        </div>
        <div class="card-body">
            @if (Model.Rules.Any())
            {
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Type</th>
                                <th>Applicability</th>
                                <th>Rate</th>
                                <th>Fixed Fee</th>
                                <th>Effective Dates</th>
                                <th>Priority</th>
                                <th>Status</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var rule in Model.Rules)
                            {
                                <tr class="@(!rule.IsActive ? "table-secondary" : "")">
                                    <td>
                                        <strong>@rule.Name</strong>
                                        @if (rule.Notes != null)
                                        {
                                            <br/>
                                            <small class="text-muted">@rule.Notes</small>
                                        }
                                    </td>
                                    <td>
                                        @if (rule.ApplicabilityType == CommissionRuleApplicability.Global)
                                        {
                                            <span class="badge bg-primary">Global</span>
                                        }
                                        else if (rule.ApplicabilityType == CommissionRuleApplicability.Category)
                                        {
                                            <span class="badge bg-success">Category</span>
                                        }
                                        else if (rule.ApplicabilityType == CommissionRuleApplicability.Seller)
                                        {
                                            <span class="badge bg-info">Seller</span>
                                        }
                                        else if (rule.ApplicabilityType == CommissionRuleApplicability.SellerTier)
                                        {
                                            <span class="badge bg-warning">Tier</span>
                                        }
                                    </td>
                                    <td>
                                        @if (rule.ApplicabilityType == CommissionRuleApplicability.Category && rule.Category != null)
                                        {
                                            <text>@rule.Category.Name</text>
                                        }
                                        else if (rule.ApplicabilityType == CommissionRuleApplicability.Seller && rule.Store != null)
                                        {
                                            <text>@rule.Store.StoreName</text>
                                        }
                                        else if (rule.ApplicabilityType == CommissionRuleApplicability.SellerTier)
                                        {
                                            <text>@rule.SellerTier</text>
                                        }
                                        else
                                        {
                                            <text>All</text>
                                        }
                                    </td>
                                    <td>@rule.CommissionPercentage.ToString("F2")%</td>
                                    <td>$@rule.FixedCommissionAmount.ToString("F2")</td>
                                    <td>
                                        <small>
                                            <strong>Start:</strong> @rule.EffectiveStartDate.ToString("yyyy-MM-dd")<br/>
                                            <strong>End:</strong> @(rule.EffectiveEndDate?.ToString("yyyy-MM-dd") ?? "No end date")
                                        </small>
                                    </td>
                                    <td>
                                        <span class="badge bg-secondary">@rule.Priority</span>
                                    </td>
                                    <td>
                                        @if (rule.IsActive)
                                        {
                                            if (rule.EffectiveStartDate > DateTime.UtcNow.Date)
                                            {
                                                <span class="badge bg-info">Scheduled</span>
                                            }
                                            else if (rule.EffectiveEndDate.HasValue && rule.EffectiveEndDate < DateTime.UtcNow.Date)
                                            {
                                                <span class="badge bg-secondary">Expired</span>
                                            }
                                            else
                                            {
                                                <span class="badge bg-success">Active</span>
                                            }
                                        }
                                        else
                                        {
                                            <span class="badge bg-danger">Inactive</span>
                                        }
                                    </td>
                                    <td>
                                        <div class="btn-group btn-group-sm" role="group">
                                            <a href="/Admin/CommissionRules/Edit?id=@rule.Id" class="btn btn-outline-primary" title="Edit">
                                                <i class="bi bi-pencil"></i>
                                            </a>
                                            <button type="button" class="btn btn-outline-danger" 
                                                    onclick="confirmDelete(@rule.Id, '@rule.Name')" title="Delete">
                                                <i class="bi bi-trash"></i>
                                            </button>
                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            }
            else
            {
                <div class="text-center py-5">
                    <i class="bi bi-inbox" style="font-size: 3rem; color: #ccc;"></i>
                    <p class="text-muted mt-3">No commission rules found.</p>
                    <a href="/Admin/CommissionRules/Create" class="btn btn-primary">
                        <i class="bi bi-plus-circle"></i> Create Your First Rule
                    </a>
                </div>
            }
        </div>
    </div>
</div>

<!-- Delete Confirmation Modal -->
<form method="post" id="deleteForm">
    @Html.AntiForgeryToken()
    <input type="hidden" name="id" id="deleteRuleId" />
    <div class="modal fade" id="deleteModal" tabindex="-1">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Confirm Delete</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to delete the commission rule <strong id="deleteRuleName"></strong>?</p>
                    <p class="text-danger">
                        <i class="bi bi-exclamation-triangle"></i>
                        This action cannot be undone. Ensure this rule is not currently in use.
                    </p>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" asp-page-handler="Delete" class="btn btn-danger">Delete Rule</button>
                </div>
            </div>
        </div>
    </div>
</form>

@section Scripts {
    <script>
        function confirmDelete(ruleId, ruleName) {
            document.getElementById('deleteRuleId').value = ruleId;
            document.getElementById('deleteRuleName').textContent = ruleName;
            var modal = new bootstrap.Modal(document.getElementById('deleteModal'));
            modal.show();
        }
    </script>
}
